import { VendorsGrid } from "@/features/vendor/vendors-grid";

export default async function VendorsPage() {
  return (
    <main className="mx-auto w-full max-w-6xl">
      <VendorsGrid />
    </main>
  );
}
